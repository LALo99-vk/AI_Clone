from agent import run_ai_agent

if __name__ == "__main__":
    # Example prompt (replace with input logic/UI integration)
    user_prompt = "Schedule meeting with Divya at 10am"
    response = run_ai_agent(user_prompt)
    print(response)
